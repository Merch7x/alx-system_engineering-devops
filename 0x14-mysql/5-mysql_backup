#!/usr/bin/env bash
#create an sql dump of all databases

if [ $# -lt 1 ];
then
	echo "Usage: Mysql password"
else
	mysqldump -u root -p "$1" --all-databases --master-data > backup.sql && tar -czvf "$(date +%d-%m-%Y).tar.gz" backup.sql
fi

